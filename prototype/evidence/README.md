# Local validation artifacts

Verification scripts write reports here. Local screenshots, client configuration snapshots, and test databases are excluded from Git.

Run the automated suite described in the [prototype guide](../README.md) to check your checkout. Historical reports and the inputs used by `scripts/record_prototype.py` remain local to the original development workspace.
